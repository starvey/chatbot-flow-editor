{
  "name": "sharvey-flow",
  "version": "1.0.0",
  "description": "A graphical flow editor",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc-watch": "tsc ./package/*.ts ./user/*.ts --outDir ./public/platform --module amd --watch",
    "server": "python server.py --port 8000 --dir ./public/;",
    "start:dev": "npm run server & npm run tsc-watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commonjs": "0.0.1"
  },
  "devDependencies": {
    "@types/es6-shim": "^0.31.34"
  }
}
